# Lumen 0.0.5 Release Checklist

Release target: `0.0.5` (`CFBundleVersion` 5), Apple Silicon, macOS 15+.

## Source and behavior

- [x] Full automated test suite passes with no failures.
- [x] Release build analysis passes with no warnings or errors.
- [x] Finder-style selection, inline rename, Return, Esc, `⌘↓`, double-click, search filtering, and failure recovery are verified.
- [x] App icon source files, canvas sizes, and subject footprint are unchanged from 0.0.4.
- [x] README and release notes describe 0.0.5 accurately.

## Artifact and update feed

- [x] `dist/Lumen-0.0.5.dmg` exists and passes `hdiutil verify`.
- [x] Mounted app reports version 0.0.5, build 5.
- [x] App and embedded Sparkle framework pass strict code-signature validation.
- [x] Public build is non-sandboxed and passes strict code-signature validation.
- [x] Appcast retains 0.0.4 and lists 0.0.5/build 5 first.
- [x] Appcast enclosure length equals the DMG byte length.
- [x] Sparkle Ed25519 signature verifies with the configured release key.
- [x] Packaged app launches successfully from the mounted DMG.

## GitHub publication

- [x] Release evidence and SHA-256 are committed on `main`.
- [x] Tag `v0.0.5` points to the verified release commit.
- [x] `main`, tag, DMG, and appcast are pushed to GitHub.
- [x] GitHub Release `v0.0.5` is public and marked latest.
- [x] Public DMG and appcast hashes match the local release artifacts.
- [x] Latest appcast publishes build 5 before build 4.
- [x] The 0.0.5 package is accepted by the Sparkle signature verifier, proving the 0.0.4 → 0.0.5 update path.

## Update authenticity

Sparkle update authenticity is independently enforced with Ed25519.
