# Lumen 0.0.6 Release Checklist

Release target: `0.0.6` (`CFBundleVersion` 6), Apple Silicon, macOS 15+.

## Source and behavior

- [x] Full automated test suite passes: 109 passed, 0 failed, and only the credential-gated real OSS smoke test skipped.
- [x] Release build analysis passes with no warnings or errors.
- [x] Object rename, folder rename, same-Bucket move, successful undo, conflict refusal, retry retention, scope gating, selection restoration, and favorite restoration are verified.
- [x] Text-editing Undo, the Edit-menu `⌘Z` command, actionable banner timing, and separate action/dismiss targets are verified.
- [x] Copy, upload, folder creation, and delete remain explicitly non-undoable.
- [x] README and release notes describe 0.0.6 accurately.

## Artifact and update feed

- [x] `dist/Lumen-0.0.6.dmg` exists and passes `hdiutil verify`.
- [x] Mounted app reports version 0.0.6, build 6, and arm64 architecture.
- [x] App and embedded Sparkle framework pass strict code-signature validation.
- [x] Public build is non-sandboxed, ad-hoc signed, and does not enable hardened runtime.
- [x] App icon source hashes, canvas dimensions, and subject footprint match v0.0.5 byte for byte.
- [x] Appcast lists build 6 first and retains builds 5 and 4.
- [x] Appcast enclosure length is 3,870,997 bytes, equal to the DMG byte length.
- [x] Sparkle Ed25519 signature verifies with the configured release key.
- [x] Packaged app launches successfully from the mounted DMG.

Local artifact SHA-256:

- `Lumen-0.0.6.dmg`: `7cbbd62a0266bebb92611918a7e3d6a8b48d924a1c41fcfa777d3e7faa5e9b57`
- `appcast.xml`: `549b7eef24aa722f4f2804577a5c91800d60d0e58560d12d6bfd4083867ee650`

## GitHub publication

- [ ] Release evidence and SHA-256 are committed on `main`.
- [ ] Tag `v0.0.6` points to the verified release commit.
- [ ] `main`, tag, DMG, and appcast are pushed to GitHub.
- [ ] GitHub Release `v0.0.6` is public and marked Latest.
- [ ] Public DMG and appcast hashes match the local release artifacts.
- [ ] Latest appcast publishes build 6 before builds 5 and 4.
- [ ] The public 0.0.6 package is accepted by Sparkle's signature verifier.

## Known distribution constraint

The public build remains ad-hoc signed and unnotarized because no Apple Developer ID identity is installed. Gatekeeper rejection on a clean first download is therefore expected and documented; Sparkle update authenticity is independently enforced with Ed25519.
